---
description: Placeholder command so Zed ACP populates the slash-command picker for this project
---

This is a placeholder so the `/` picker in Zed's Claude ACP integration is non-empty for this project. Replace or delete once you add real project commands under `.claude/commands/`.
